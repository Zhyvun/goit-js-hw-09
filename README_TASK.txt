https://www.edu.goit.global/uk/learn/7349052/2294/2303/homework

😎 Завдання 1 - перемикач кольорів

Виконуй це завдання у файлах 01-color-switcher.html і 01-color-switcher.js. 
Подивися демо-відео роботи перемикача.

HTML містить кнопки «Start» і «Stop».

<button type="button" data-start>Start</button>
<button type="button" data-stop>Stop</button>

1. Напиши скрипт, який після натискання кнопки «Start», раз на секунду змінює колір фону <body> на випадкове 
значення, використовуючи інлайн стиль. 
2. Натисканням на кнопку «Stop» зміна кольору фону повинна зупинятися.
3. Для генерування випадкового кольору використовуй функцію getRandomHexColor.

function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215).toString(16).padStart(6, 0)}`;
}
1.1. Задаємо в константі умову задачі, щодо зміни кольору через 1 секунду
 + задаємо початковий інтервал (null)
1.2. Отримуємо необхідні елементи для роботи
1.3. Всстановлюємо за замовченням неактивною кнопку Stop. Вішаємо слухачів на кнопки на "click", викликаємо функції
1.4. Створюємо функцію запуску Start
 - кнопка Stop стає  НЕ активною
 - кнопка Start стає  активною
 - активується ф-я зміни кольору через 1 секунду

 2.1. Створюємо функцію зупинку Stop
 - кнопка Stop стає  активною
 - кнопка Start стає  НЕ активною
 - очищуємо заданий інтервал функцією Start

(див Модуль 9 заняття 1)


😎 Завдання 2 - таймер зворотного відліку

Виконуй це завдання у файлах 02-timer.html і 02-timer.js. Напиши скрипт таймера, який здійснює зворотний відлік до певної дати. 
Такий таймер може використовуватися у блогах та інтернет-магазинах, сторінках реєстрації подій, під час технічного обслуговування тощо. 
Подивися демо-відео роботи таймера.

...https://www.edu.goit.global/uk/learn/7349052/2294/2303/homework

Елементи інтерфейсу

HTML містить готову розмітку таймера, поля вибору кінцевої дати і кнопку, по кліку на яку, таймер повинен запускатися. 
Додай мінімальне оформлення елементів інтерфейсу.

Бібліотека flatpickr (https://flatpickr.js.org/)

Використовуй бібліотеку flatpickr для того, щоб дозволити користувачеві кросбраузерно вибрати кінцеву дату і час в одному елементі інтерфейсу. 
Для того щоб підключити CSS код бібліотеки в проект, необхідно додати ще один імпорт, крім того, що описаний в документації.

// Описаний в документації
import flatpickr from "flatpickr";
// Додатковий імпорт стилів
import "flatpickr/dist/flatpickr.min.css";

Бібліотека очікує, що її ініціалізують на елементі input[type="text"], тому ми додали до HTML документу поле input#datetime-picker.

<input type="text" id="datetime-picker" />

Другим аргументом функції flatpickr(selector, options) можна передати необов'язковий об'єкт параметрів. 
Ми підготували для тебе об'єкт, який потрібен для виконання завдання. Розберися, 
за що відповідає кожна властивість в документації «Options», і використовуй його у своєму коді.

const options = {
  enableTime: true,
  time_24hr: true,
  defaultDate: new Date(),
  minuteIncrement: 1,
  onClose(selectedDates) {
    console.log(selectedDates[0]);
  },
};

Вибір дати

Метод onClose() з об'єкта параметрів викликається щоразу під час закриття елемента інтерфейсу, який створює flatpickr. 
Саме у ньому варто обробляти дату, обрану користувачем. Параметр selectedDates - це масив обраних дат, тому ми беремо перший елемент.

    Якщо користувач вибрав дату в минулому, покажи window.alert() з текстом 
    "Please choose a date in the future".
    Якщо користувач вибрав валідну дату (в майбутньому), кнопка «Start» стає активною.
    Кнопка «Start» повинна бути неактивною доти, доки користувач не вибрав дату в майбутньому.
    Натисканням на кнопку «Start» починається відлік часу до обраної дати з моменту натискання.

Відлік часу

Натисканням на кнопку «Start» скрипт повинен обчислювати раз на секунду, скільки часу залишилось до вказаної дати, 
і оновлювати інтерфейс таймера, показуючи чотири цифри: дні, години, хвилини і секунди у форматі xx:xx:xx:xx.

    Кількість днів може складатися з більше, ніж двох цифр.
    Таймер повинен зупинятися, коли дійшов до кінцевої дати, тобто 00:00:00:00.

*Не будемо ускладнювати
Якщо таймер запущений, для того щоб вибрати нову дату і перезапустити його - необхідно перезавантажити сторінку.

Для підрахунку значень використовуй готову функцію convertMs, де ms - різниця між кінцевою і поточною датою в мілісекундах.

function convertMs(ms) {
  // Number of milliseconds per unit of time
  const second = 1000;
  const minute = second * 60;
  const hour = minute * 60;
  const day = hour * 24;

  // Remaining days
  const days = Math.floor(ms / day);
  // Remaining hours
  const hours = Math.floor((ms % day) / hour);
  // Remaining minutes
  const minutes = Math.floor(((ms % day) % hour) / minute);
  // Remaining seconds
  const seconds = Math.floor((((ms % day) % hour) % minute) / second);

  return { days, hours, minutes, seconds };
}

console.log(convertMs(2000)); // {days: 0, hours: 0, minutes: 0, seconds: 2}
console.log(convertMs(140000)); // {days: 0, hours: 0, minutes: 2, seconds: 20}
console.log(convertMs(24140000)); // {days: 0, hours: 6 minutes: 42, seconds: 20}

Форматування часу
Функція convertMs() повертає об'єкт з розрахованим часом, що залишився до кінцевої дати. 
Зверни увагу, що вона не форматує результат. Тобто, якщо залишилося 4 хвилини або будь-якої іншої складової часу, 
то функція поверне 4, а не 04. В інтерфейсі таймера необхідно додавати 0, якщо в числі менше двох символів. 
Напиши функцію addLeadingZero(value), яка використовує метод padStart() і перед рендерингом інтефрейсу форматує значення.

❗ Увага
Наступний функціонал не обов'язковий для здавання завдання, але буде хорошою додатковою практикою.
Для відображення повідомлень користувачеві, замість window.alert(), використовуй бібліотеку notiflix 
(https://github.com/notiflix/Notiflix#readme)

Виконання:
1.Підключаємо та імпортуємо бібліотеки: 
1.1.  flatpickr + flatpickr.min.css   (npm i flatpickr --save)
1.2. notiflix  (npm i notiflix)

2. Створюємо змінні та прописуємо константи за замовчуванням.

3. Отримуємо поля з HTML для роботи
3.1.Ініціалізуємо бібліотеку flatpickr на елементі input[type="text"]
3.2 Деактивуємо кнопку старт + Вішаемо на неї слухача і викликаємо функцію відліку часу
3.3. Функція календаря "const options" 
  enableTime: true, - (Boolean) Вмикає засіб вибору часу
  time_24hr: true, -  (Boolean) вибір формату часу в 24-годинному режимі
  defaultDate: new Date(), - (String) Встановлює початкові вибрані дати
  minuteIncrement: 1,  - (Integer) Регулює крок для введення хвилин (включно з прокручуванням)
  onClose(selectedDates) Функції, яка запускаються щоразу, коли календар закривається 
  (selectedDates - масив об'єктів Date, вибраних користувачем. Якщо дати не вибрано, масив порожній.)
    console.log(selectedDates[0]);
 3.4. Встановлюємо поточну дату з налаштувань користувача + Виводимо "інструкцію" з подальшими діями.

 4. Функція onDateCheck - обираємо потрібну дату.
 - за замовчуванням поточна дата+час
 - дозволяємо користувачу встановити дату та час
 - якщо обрані дата+час більші за поточний виводимо повідомлення і активуємо кнопку "Start"
 - інакше виводимо поідомлення "Please choose a date in the future"

 5. Функція timerStart - запускаємо таймер у зворотньому напрямку
 - після підрахунку виводимо повідомлення про завершення, деактивуємо кнопку і активуємо поле вибору дати
 - під час підрахунку від обраної дати віднімаємо поточний дату+час, всі дані конвертуємо в мілісекунди
 - крок підрахунку 1 секунда

 6.Функція createMarkup - записуємо дані на сторінку 

 7. Приводимо формат чисел до 2-х значних
метод padStart(кількість знаків, число яке треба додати) 
приймає виключно строку тому трансформуємо число в строку String(value), 
застосовуємо метод padStart () і повертаємо return результат

8. Функція convertMs - конвертування мілісекунд у дні, часи, хвилини, секунди
мілісекунд в секунді (second) * 
секунд в хвилині (minute) *
хвилин в годині (hour) *
годин в дні (day) 
формула Math.floor виводить тільки цілі числа округлені в низ
повертаємо {дні, часи, хвилини, секунди}

😎 Завдання 3 - генератор промісів
Виконуй це завдання у файлах 03-promises.html і 03-promises.js. 
Подивися демо-відео роботи генератора промісів.
....

HTML містить розмітку форми, в поля якої користувач буде вводити 
першу затримку в мілісекундах, крок збільшення затримки для кожного 
промісу після першого і кількість промісів, яку необхідно створити.

Напиши скрипт, який на момент сабміту форми викликає функцію createPromise(position, delay) стільки разів, 
скільки ввели в поле amount. Під час кожного виклику передай їй номер промісу (position), що створюється, і затримку, 
враховуючи першу затримку (delay), введену користувачем, і крок (step).

function createPromise(position, delay) {
  const shouldResolve = Math.random() > 0.3;
  if (shouldResolve) {
    // Fulfill
  } else {
    // Reject
  }
}

Доповни код функції createPromise таким чином, щоб вона повертала один проміс, який виконується або відхиляється через delay часу. 
Значенням промісу повинен бути об'єкт, в якому будуть властивості position і delay зі значеннями однойменних параметрів. Використовуй початковий код функції для вибору того,
що потрібно зробити з промісом - виконати або відхилити.

createPromise(2, 1500)
  .then(({ position, delay }) => {
    console.log(`✅ Fulfilled promise ${position} in ${delay}ms`);
  })
  .catch(({ position, delay }) => {
    console.log(`❌ Rejected promise ${position} in ${delay}ms`);
  });

❗ Наступний функціонал не обов'язковий для здавання завдання, але буде хорошою додатковою практикою.

Для відображення повідомлень користувачеві, замість console.log(), використовуй бібліотеку notiflix.

Виконання :
https://www.youtube.com/watch?v=eAoKjIcLP4Y
00:10:40
1. Змінюємо умрви сповіщення бібліотеки notiflix за замовчуванням  (https://github.com/notiflix/Notiflix#readme)

2. function createPromise - відповідає за створення промісу. Має повертатись новий Promise + delay (це наш об'єкт),  
і має бути виконано через якийсь проміжок часу, тому в середині буде працювати таймер (setTimeout), 
оскільки для кожного промісу небхідно віпрацювати лиле один раз. 
Функція працює у циклі і для коректного відпрацьовування вона має повертати в циклі новий проміс кожного разу тому відпрацьовує return + створення нового промісу
- змінна shouldResolve - дає значення через рандомайзер
- повертаємо новий проміс який буде отримувати через callback дві ф-ї:
• успішне відпрацювання - resolve → (відпрацьовуємо then)
• помилка reject → (відпрацьовуємо cath)
- для того, щоб ренедерилося для кожного промісу одинраз із відкладеним часом  
прописуємо setTimeout (не приймає ніяких параметрів) але в умову передаємо однакові об'єкти (position, delay),
 з метою маркування який проміс відпрацював успішно (його позиція і черз який час), а який був відхилений, тобто
- якщо  resolve предаємо (position, delay)
- якщо  reject предаємо (position, delay)
- ❗ ВАЖЛИВО - працюючи із setTimeout маємо виставити delay (вираховується динамічно)

3. onPromiseCreate
- скасовуємо дії браузера за замовчуванням
- робимо деструктуризацію елементів форми (звертаємось до полів елементів форми)
- отримуємо дані з полів і приводио їх до потрібного нам типу(number) і забираємо значення з отриманих полів (dalay.value)
- 00:44:39 - ф-я For (відповідає за ренедер) "Let i" можна починати:
• або  з "0" тоді  i < inputAmount
• або з "1" тоді i <= inputAmount
- в середині for викликаємо createPromise і передаємо на місця пераметрів (position, delay) відповідні аргументи
 position - передали позицію на якій має бути проміс відслідковування відпрацювання проміса (чи він був успішно завершений, чи відхилений)
- 01:08:15 then - передається   ... в консоль виокристовуємо ті назви параметрів, які передаються при виклику (в createPromise вже приходить те що потрібно просто воно прокидується далі на місця ) 
 
 01:10:48 Умови: перший проміс має відредеритися після delay, а всі наступні мають бути відренедерені через початковий delay + step, є два варіанти:
  
 • в кікці циклу - тобто переший delay відпрацював і в кінці циклу робимо (inputDelay += inputStep) і таким чином перша ітерація циклу пройшла з inputDelay початкове відкладення, 
 потім закінчився цикл і останнім кроком додаємо  inputStep і наступна ітерація буде починатись із оновленого delay (не з 1-ї секунди а з 1,2 секунди і т.д.) - більш зрозуміліший;
 • на початку циклу - вираховуємо  (inputDelay + inputStep * i) і таким чином коли ми помножимо на "0" то відпрацює його базове значення, а наступник короком у нас і-те буде "1" тому input*1 і додаємо і так по колу рухаємся 
 обидваваріанти валідні 


 01:35:55 Імпортуємо бібліотеку 
 4. 01:52:48 Додаткова перевірка на від'ємні числа 